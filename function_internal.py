print(dir())
import random # random 모듈 가져다 쓰기
print(dir())
import pickle # pickle 모듈 가져다 쓰기
print(dir())

import random
print(dir(random)) #랜덤 모듈내에서 사용가능한 함수 출력

lst = [1, 2, 3]
print(dir(lst)) #리스트에서 사용가능한 함수 출력